[build]
  command = "npm install && node set-env.js && npx ng build --configuration production"
  publish = "dist/frontend/browser"

# SPA fallback — redirect all routes to index.html for Angular routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
